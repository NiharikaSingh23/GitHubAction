name: second new workflow
on:
  push:
    branches: ["main"]
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: welcome user
        run: echo build is starting
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: welcome user
        run: echo test is starting
   
  deploye:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: welcome user
        run: deploye is starting
      
